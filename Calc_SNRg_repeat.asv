% Script to run CalculateSNRg.m multiple times for OutputFile2 or OutputFile3

clear;
clear global;
clc;
close all;

OutputFile = 3; % Choose 2 or 3

%% OutputFile2
if (O)
for TSTrial = 1:10
    run CalculateSNRg.m
end

%% OutputFile3
global StepSize_in; %#ok<*GVMIS>
StepSize_invec = [0.25*0.125, 0.5*0.125, 0.125, 0.25, 0.5, 0.75, 1, 1.5, 2, 2.5, 3]*0.05/10000;
for it = 2:length(StepSize_invec)
    StepSize_in = StepSize_invec(it);
    run CalculateSNRg.m
end 
clear global;
